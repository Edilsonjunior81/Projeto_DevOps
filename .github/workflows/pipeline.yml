name: Secure CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Instalar Python e dependências
        run: |
          python3 --version
          pip install pytest

      - name: Rodar testes
        run: pytest || echo "Sem testes implementados"

  sast:
    name: SAST - Semgrep
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Instalar Semgrep
        run: pip install semgrep

      - name: Executar análise de SAST
        run: semgrep --config=auto .
        continue-on-error: false

  dast:
    name: DAST - OWASP ZAP
    runs-on: ubuntu-latest
    needs: sast

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3
        
      - name: Iniciar app para o ZAP testar
        run: |
          python3 Hello_word.py &
          sleep 5
      
      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.8.0
        with:
          target: "http://localhost:8080"
name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Executar comando simples
        run: echo "Pipeline executada com sucesso!"
